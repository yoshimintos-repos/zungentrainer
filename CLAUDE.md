# CLAUDE.md

This repository now uses `AGENTS.md` as the canonical agent guidance file for
Codex.

Claude Code users should read `AGENTS.md` and follow the same project
instructions. Keep durable guidance in `AGENTS.md` to avoid drift between
agent-specific files.
